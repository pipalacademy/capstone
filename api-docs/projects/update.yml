endpoint: /api/projects/<project-slug>
method: PUT
title: Update a single project
description: Update a single project. Its ID is given by the slug, and the body should have all the project fields as a JSON.
  This will return a status 404 response when a project with this slug doesn't exist. This will return a status 400 response
  when the body doesn't have all the required fields.
authenticated: true
request:
  path_parameters:
    - name: project-slug
      type: string
      description: The unique project slug to use to fetch details about a project.
      example: build-your-own-shell
      required: true
  query_parameters: []
  body:
    type: object
    example: |
      {
        "title": "build your own shell",
        "short_description": "a short description of the project, in markdown format. this is displayed on the project's card on the home page and dashboard.",
        "description": "in this project, we will build a unix shell from scratch.\n\nwe'll use the python's `subprocess` library to build shell.\n\n# learning outcomes\n- unix\n- python\n",
        "tags": ["Python", "Unix"],
        "tasks": [
            {
              "slug": "stdin-and-stdout",
              "position": 1,
              "title": "stdin and stdout",
              "description": "take input from stdin and simply echo it to stdout in a loop."
            },
            {
              "slug": "write-a-parser",
              "position": 2,
              "title": "write a parser",
              "description": "write a parser for shell. this part of description can include *italic*, **bold**, `code`, and other markdown formatting."
            }
        ]
      }
    properties:
      - name: title
        type: string
        description: Human readable title of the project
        example: Build your own Shell
        required: true
      - name: short_description
        type: string
        description: |
          A short description of the project, in Markdown format. This is displayed
          on the Project's card on the home page and dashboard. example: Learn the internals of Unix system by building your own Unix shell.
        required: true
      - name: description
        type: string
        description: |
          A longer description of the project in Markdown format. This is displayed
          on the Project's own page in the UI.
        example: |
          In this project, we will build a Unix shell from scratch.

          We'll use the Python's `subprocess` library to build shell.

          # Learning outcomes
          - Unix
          - Python
        required: true
      - name: is_active
        type: boolean
        description: Whether the project is active and available for users to take up. (optional, defaults to `true`)
        required: true
        default: true
      - name: tags
        type: array[string]
        description: Tags associated with this project.
        each:
          - type: string
        example: |
          ["Python", "Unix"]
        required: true
      - name: tasks
        type: array[tasks]
        description: Array of tasks associated with this Project
        required: true
        each:
          - type: object
            properties:
              - name: slug
                type: string
                description: The unique identifier for this task, as defined by the course creator.
                example: "write-a-parser"
                required: true
              - name: position
                type: Integer
                description: The position of the Task in sequence
                example: 2
                required: true
              - name: title
                type: string
                description: Title of the Task
                example: "Write a parser"
                required: true
              - name: description
                type: string
                description: Description of the task in markdown format.
                example: |
                  Write a parser for shell. This part of description can
                  include *italic*, **bold**, `code`, and other markdown formatting.
                required: true
            example: |
              {
                "slug": "write-a-parser",
                "position": 2,
                "title": "Write a parser",
                "description": "Write a parser for shell. This part of description can include *italic*, **bold**, `code`, and other markdown formatting.",
              }
response:
  - type: object
    description: The update Project object (status 200), or an error (status 4XX)
    example: |
      {
        "slug": "build-your-own-shell",
        "title": "build your own shell",
        "short_description": "a short description of the project, in markdown format. this is displayed on the project's card on the home page and dashboard.",
        "description": "in this project, we will build a unix shell from scratch.\n\nwe'll use the python's `subprocess` library to build shell.\n\n# learning outcomes\n- unix\n- python\n",
        "is_active": true,
        "tags": ["Python", "Unix"],
        "tasks": [
            {
              "slug": "stdin-and-stdout",
              "position": 1,
              "title": "stdin and stdout",
              "description": "take input from stdin and simply echo it to stdout in a loop."
            },
            {
              "slug": "write-a-parser",
              "position": 2,
              "title": "write a parser",
              "description": "write a parser for shell. this part of description can include *italic*, **bold**, `code`, and other markdown formatting."
            }
        ]
      }
    properties:
      - name: slug
        type: string
        description: Project slug that can be used in API requests and URLs. Should not exist already. If it does, response will be a 400 Bad Request.
        example: build-your-own-shell
        required: true
      - name: title
        type: string
        description: Human readable title of the project
        example: Build your own Shell
        required: true
      - name: short_description
        type: string
        description: |
          A short description of the project, in Markdown format. This is displayed
          on the Project's card on the home page and dashboard.
        example: Learn the internals of Unix system by building your own Unix shell.
        required: true
      - name: description
        type: string
        description: |
          A longer description of the project in Markdown format. This is displayed
          on the Project's own page in the UI.
        example: |
          In this project, we will build a Unix shell from scratch.

          We'll use the Python's `subprocess` library to build shell.

          # Learning outcomes
          - Unix
          - Python
        required: true
      - name: is_active
        type: boolean
        description: Whether the project is active and available for users to take up.
        required: true
      - name: tags
        type: array[string]
        description: Tags associated with this project.
        each:
          - type: string
        example: |
          ["Python", "Unix"]
        required: true
      - name: tasks
        type: array[tasks]
        description: Array of tasks associated with this Project
        required: true
        each:
          - type: object
            properties:
              - name: slug
                type: string
                description: The unique identifier for this task, as defined by the course creator.
                example: "write-a-parser"
                required: true
              - name: position
                type: Integer
                description: The position of the Task in sequence
                example: 2
                required: true
              - name: title
                type: string
                description: Title of the Task
                example: "Write a parser"
                required: true
              - name: description
                type: string
                description: Description of the task in markdown format.
                example: |
                  Write a parser for shell. This part of description can
                  include *italic*, **bold**, `code`, and other markdown formatting.
                required: true
            example: |
              {
                "slug": "write-a-parser",
                "position": 2,
                "title": "Write a parser",
                "description": "Write a parser for shell. This part of description can include *italic*, **bold**, `code`, and other markdown formatting.",
              }
